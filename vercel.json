{
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.4 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @chroma/front-store build",
  "outputDirectory": "packages/front-store/dist"
}
